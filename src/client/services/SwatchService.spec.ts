import SwatchService from './SwatchService'

const service = new SwatchService

describe('SwatchService', () => {
    it('should have a fetch method', () => {
        // SwatchService
        // assert(1)
        console.log('OK');
    })
})

